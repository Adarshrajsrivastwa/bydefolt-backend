/**
 * @deprecated Use {@link Follower} (`from` / `to`, same pattern as Connection).
 * Legacy `companyfollows` collection is migrated automatically on first read.
 */
export { Follower as CompanyFollow } from './Follower.js';
